backend: api
backend_options:
  auto_create_first_user: test
  title_generation:
    provider: fake_llm
database: 'sqlite:///:memory:'
model:
  default_preset: test
plugins:
  enabled:
    - provider_fake_llm
