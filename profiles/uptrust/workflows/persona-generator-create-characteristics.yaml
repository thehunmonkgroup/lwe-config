---
- name: "Pick random value for characteristic: {{ key }}"
  set_fact:
    value: "{{ data[key] | random }}"

- name: "Add  '{{ key }}: {{ value }}' to characteristics list"
  set_fact:
    characteristics: "{{ characteristics + [key + ': ' + value | trim] }}"
...
